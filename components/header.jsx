// components/header.jsx
// A clean, focused brand bar for SoftFrequency

export function Header() {
  return (
    <header className="w-full py-6 sm:py-10 text-center">
      <a
        href="/"
        className="text-3xl sm:text-4xl font-semibold tracking-tight hover:opacity-90 transition-opacity"
      >
        🌿 SoftFrequency
      </a>
    </header>
  );
}
